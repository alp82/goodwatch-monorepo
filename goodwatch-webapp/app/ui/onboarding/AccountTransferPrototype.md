# Account-transfer presentation decision

The user accepted the preview with two changes: omit “guest” from user-facing language and preselect all items, including score replacements and preferences. The earlier unchecked defaults are superseded.

The throwaway implementation is preserved in commit `a19f0ea` on `prototype/connected-exploration`. Its development-only preview route is removed. See [Account transfer and recovery](./AccountTransfer.md) for the real implementation, browser evidence and remaining live email-confirmation checkpoint.
