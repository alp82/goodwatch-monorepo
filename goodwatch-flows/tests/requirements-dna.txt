# Generation tests use the generator's dependency set and an in-memory MongoDB.
# Windmill resolves each script separately; combining multiple complete lockfiles
# can require contradictory versions of their shared transitive dependencies.
-r ../windmill/f/dna/generate/fetch.script.lock
mongomock==4.3.0
fakeredis[lua]==2.26.2
freezegun==1.5.5
PyYAML==6.0.3
