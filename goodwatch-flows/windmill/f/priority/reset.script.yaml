summary: Acknowledge successfully published priority claims
description: Retains demand received during crawling and rejects expired leases
lock: '!inline f/priority/reset.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    publication_result:
      type: object
      description: Successful publication summary, including deferred country retries
      default: null
    claims:
      type: array
      default: null
      items:
        type: object
      originalType: record[]
  required:
    - claims
